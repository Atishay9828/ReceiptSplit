"""
app.split — ReceiptSplit Split Engine

Pure financial calculation module. Zero DB access, zero I/O.
Deterministic: same input always produces same output.

Entry point: SplitCalculator.calculate(SplitInput) -> SplitResult
"""
